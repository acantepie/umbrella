<!DOCTYPE html>

<html lang="{{ app.request.locale }}">

<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="icon" type="image/x-icon" href="{{ asset('favicon.ico') }}" />
    <title>My App</title>
    {{ encore_entry_link_tags('front') }}

    <style type="text/css">
        .logo {
            height: 40vmin;
            pointer-events: none;
        }

        header {
            background-color: #282c34;
            min-height: 100vh;
            color: white;
        }

        .links a {
            padding: 0.5rem;
        }

        a, a:hover, a:active, a:visited {
            color: #43B6C2;
        }

        a.btn-default {
            background-color: #43B6C2;
            color: #282c34;
        }

        a.btn-default:hover {
            background-color: #399aa4;
        }
    </style>
</head>

<body>

    <div class="text-center">
        <header class="d-flex flex-column align-items-center justify-content-center">
            <img src="{{ asset('build/images/umbrella.png') }}" class="logo" alt="logo">
            <h2 class="mb-4">Welcome on your Symfony 5 app</h2>

            <div>Essential links</div>
            <div class="d-flex links mb-5">
                <a href="https://symfony.com/doc/current/index.html" target="_blank">Doc Symfony</a>
                <a href="https://github.com/acantepie/umbrella" target="_blank">Doc Umbrella</a>
                <a href="https://umbrella-corp.dev/" target="_blank">Showcase Umbrella</a>
            </div>

            <a class="btn btn-default" href="/admin">
                <i class="mdi mdi-security mr-1"></i> Go to the back-office
            </a>
        </header>
    </div>

    {{ encore_entry_script_tags('front') }}
</body>

</html>

