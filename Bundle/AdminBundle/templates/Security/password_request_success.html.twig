{% extends '@UmbrellaAdmin/Security/layout.html.twig' %}

{% trans_default_domain 'UmbrellaAdmin' %}
{% block content %}
    <div class="card">
        {% include '@UmbrellaAdmin/Security/head.html.twig' %}

        <div class="card-body p-4">

            <div class="text-center m-auto">
                <img src="{{ asset('bundles/umbrellaadmin/images/mail_sent.svg') }}" alt="mail sent image" height="64">
                <h4 class="text-dark-50 text-center mt-4 font-weight-bold">{{ 'action.check_your_email' | trans }}</h4>
                <p class="text-muted mb-4">
                    {{ 'security_page.request_password_success' | trans({'%email%' : email}) | raw }}
                </p>

                <a href="{{ path(constant('Umbrella\\AdminBundle\\Controller\\SecurityController::LOGIN_ROUTE')) }}" class="btn btn-primary">
                    <i class="mdi mdi-home me-1"></i> {{ 'security_page.back_login' | trans }}
                </a>

            </div>
        </div>
    </div>
{% endblock %}