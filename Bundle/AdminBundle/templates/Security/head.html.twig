<div class="card-header pt-3 pb-3 text-center bg-primary">
    {% if admin_app_logo() %}
        <img src="{{ asset(admin_app_logo()) }}">
    {% else %}
        <h4>{{ admin_app_name() }}</h4>
    {% endif %}
</div>